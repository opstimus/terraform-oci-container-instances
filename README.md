> ⚠️ **Moved to the monorepo.**
> This module now lives in [`opstimus/terraform-modules`](https://github.com/opstimus/terraform-modules) at `modules/oci-container-instances`.
>
> ```hcl
> source = "git::https://github.com/opstimus/terraform-modules.git//modules/oci-container-instances?ref=oci-container-instances/v1.0.0"
> ```
>
> This repository remains for existing consumers; new development happens in the monorepo.

# terraform-oci-container-instances
terraform-oci-container-instances
